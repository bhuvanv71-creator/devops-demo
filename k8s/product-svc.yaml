apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-svc
  namespace: ecommerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-svc
  template:
    metadata:
      labels:
        app: product-svc
    spec:
      containers:
        - name: product-svc
          image: 598096478246.dkr.ecr.ap-south-1.amazonaws.com/product-svc:7433efefa3f413c1c70ceba42f1d45f494243e4d
          ports:
            - containerPort: 5000
          env:
            - name: PORT
              value: "5000"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: product-svc
  namespace: ecommerce
spec:
  selector:
    app: product-svc
  ports:
    - port: 5000
      targetPort: 5000
  type: ClusterIP
